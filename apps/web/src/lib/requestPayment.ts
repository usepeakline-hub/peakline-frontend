/** Fake shareable request link — stands in for a real per-request page
 * until the backend can mint one. Same pattern as `lib/receive.ts`'s
 * payment link, just under a different path. */
export const FAKE_REQUEST_LINK = "https://peakline.com/request/john-doe";
